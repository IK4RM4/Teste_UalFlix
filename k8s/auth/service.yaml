apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: ualflix
  labels:
    app: auth
    component: backend
spec:
  selector:
    app: auth
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
  type: ClusterIP
  sessionAffinity: None
